# LOG — Trigger deploy Easypanel

## Contexto
Pedido para subir tudo para deploy.

## Situação
- Código de features já estava em `origin/main` (`0f29fdf`).
- Disparado empty commit `69d3b11` para redeploy no Easypanel.
- `package-lock.json` local ignorado (projeto usa `bun.lock`).
- Pasta órfã `src/components/icons/` removida (ícone já em `chat/`).

## Keywords
deploy, easypanel, trigger
